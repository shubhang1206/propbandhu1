<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Propbandhu</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        .property-card {
            transition: transform 0.2s, box-shadow 0.2s;
        }
        .property-card:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.1);
        }
        .status-badge {
            position: absolute;
            top: 12px;
            left: 12px;
            z-index: 10;
        }
    </style>
</head>
<body class="bg-gray-50">
    <!-- Broker Navigation -->
    <nav class="bg-white shadow-lg">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between h-16">
                <div class="flex items-center">
                    <a href="/broker/dashboard" class="flex items-center">
                        <span class="text-xl font-bold text-gray-900">Propbandhu Broker</span>
                    </a>
                </div>
                <div class="flex items-center space-x-4">
                    <span class="text-gray-700 font-medium">🤝 Broker: <%= user.name %></span>
                    <a href="/broker/dashboard" class="text-gray-600 hover:text-blue-600">
                        <i class="fas fa-tachometer-alt mr-1"></i>Dashboard
                    </a>
                    <a href="/broker/properties" class="text-gray-600 hover:text-blue-600 <%= activePage === 'broker' ? 'text-blue-600 font-semibold' : '' %>">
                        <i class="fas fa-home mr-1"></i>Properties
                    </a>
                    <a href="/broker/commissions" class="text-gray-600 hover:text-blue-600">
                        <i class="fas fa-money-bill-wave mr-1"></i>Commissions
                    </a>
                    <a href="/logout" class="bg-red-500 text-white px-4 py-2 rounded-lg hover:bg-red-600">
                        <i class="fas fa-sign-out-alt mr-1"></i>Logout
                    </a>
                </div>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <div class="max-w-7xl mx-auto py-6 sm:px-6 lg:px-8">
        <!-- Page Header -->
        <div class="mb-8">
            <div class="flex justify-between items-center mb-6">
                <div>
                    <h1 class="text-3xl font-bold text-gray-900">My Properties</h1>
                    <p class="text-gray-600 mt-2">Manage properties you've added or are assigned to</p>
                </div>
                <a href="/broker/properties/add" class="bg-blue-600 text-white px-6 py-3 rounded-lg font-semibold hover:bg-blue-700 transition">
                    <i class="fas fa-plus-circle mr-2"></i>Add New Property
                </a>
            </div>

            <!-- Filters -->
            <div class="bg-white rounded-xl shadow p-6 mb-6">
                <form method="GET" class="grid grid-cols-1 md:grid-cols-4 gap-4">
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-2">Status</label>
                        <select name="status" class="w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-blue-500 focus:border-blue-500">
                            <option value="all" <%= filters.status === 'all' ? 'selected' : '' %>>All Status</option>
                            <option value="pending_approval" <%= filters.status === 'pending_approval' ? 'selected' : '' %>>Pending Approval</option>
                            <option value="approved" <%= filters.status === 'approved' ? 'selected' : '' %>>Approved</option>
                            <option value="live" <%= filters.status === 'live' ? 'selected' : '' %>>Live</option>
                            <option value="sold" <%= filters.status === 'sold' ? 'selected' : '' %>>Sold</option>
                            <option value="rejected" <%= filters.status === 'rejected' ? 'selected' : '' %>>Rejected</option>
                        </select>
                    </div>
                    
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-2">Type</label>
                        <select name="type" class="w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-blue-500 focus:border-blue-500">
                            <option value="all" <%= filters.type === 'all' ? 'selected' : '' %>>All Types</option>
                            <option value="added" <%= filters.type === 'added' ? 'selected' : '' %>>Added by Me</option>
                            <option value="assigned" <%= filters.type === 'assigned' ? 'selected' : '' %>>Assigned to Me</option>
                        </select>
                    </div>
                    
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-2">Sort By</label>
                        <select name="sort" class="w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-blue-500 focus:border-blue-500">
                            <option value="createdAt" <%= filters.sort === 'createdAt' ? 'selected' : '' %>>Date Added</option>
                            <option value="price" <%= filters.sort === 'price' ? 'selected' : '' %>>Price</option>
                            <option value="title" <%= filters.sort === 'title' ? 'selected' : '' %>>Title</option>
                        </select>
                    </div>
                    
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-2">Order</label>
                        <select name="order" class="w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-blue-500 focus:border-blue-500">
                            <option value="desc" <%= filters.order === 'desc' ? 'selected' : '' %>>Descending</option>
                            <option value="asc" <%= filters.order === 'asc' ? 'selected' : '' %>>Ascending</option>
                        </select>
                    </div>
                    
                    <div class="md:col-span-4 flex justify-end space-x-3 mt-4">
                        <button type="submit" class="bg-blue-600 text-white px-6 py-2 rounded-lg font-semibold hover:bg-blue-700 transition">
                            <i class="fas fa-filter mr-2"></i>Apply Filters
                        </button>
                        <a href="/broker/properties" class="bg-gray-200 text-gray-800 px-6 py-2 rounded-lg font-semibold hover:bg-gray-300 transition">
                            <i class="fas fa-times mr-2"></i>Clear Filters
                        </a>
                    </div>
                </form>
            </div>
        </div>

        <!-- Properties Grid -->
        <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
            <!-- Properties List -->
            <div class="lg:col-span-2">
                <% if (properties.length === 0) { %>
                    <div class="bg-white rounded-xl shadow p-12 text-center">
                        <i class="fas fa-home text-gray-300 text-6xl mb-4"></i>
                        <h3 class="text-xl font-semibold text-gray-700 mb-2">No properties found</h3>
                        <p class="text-gray-600 mb-6">Add your first property or check back later for assigned properties.</p>
                        <a href="/broker/properties/add" class="inline-flex items-center px-6 py-3 bg-blue-600 text-white font-semibold rounded-lg hover:bg-blue-700 transition">
                            <i class="fas fa-plus-circle mr-2"></i>Add Your First Property
                        </a>
                    </div>
                <% } else { %>
                    <div class="space-y-6">
                        <% properties.forEach(property => { 
                            const statusColors = {
                                'pending_approval': 'bg-yellow-100 text-yellow-800',
                                'approved': 'bg-blue-100 text-blue-800',
                                'live': 'bg-green-100 text-green-800',
                                'sold': 'bg-purple-100 text-purple-800',
                                'rejected': 'bg-red-100 text-red-800'
                            };
                            const statusColor = statusColors[property.status] || 'bg-gray-100 text-gray-800';
                        %>
                        <div class="bg-white rounded-xl shadow-lg overflow-hidden property-card">
                            <div class="p-6">
                                <div class="flex flex-col md:flex-row gap-6">
                                    <!-- Property Image -->
                                    <div class="md:w-1/3 relative">
                                        <% if (property.primary_image) { %>
                                            <img src="<%= property.primary_image %>" 
                                                 alt="<%= property.title %>" 
                                                 class="w-full h-48 object-cover rounded-lg">
                                        <% } else { %>
                                            <div class="w-full h-48 bg-gray-200 rounded-lg flex items-center justify-center">
                                                <i class="fas fa-home text-gray-400 text-4xl"></i>
                                            </div>
                                        <% } %>
                                        
                                        <!-- Status Badge -->
                                        <div class="status-badge">
                                            <span class="inline-flex items-center px-3 py-1 rounded-full text-sm font-medium <%= statusColor %>">
                                                <%= property.status.replace('_', ' ') %>
                                            </span>
                                        </div>
                                        
                                        <!-- Added By Me Badge -->
                                        <% if (property.addedByMe) { %>
                                        <div class="absolute bottom-12 left-2">
                                            <span class="inline-flex items-center px-2 py-1 rounded text-xs font-medium bg-blue-100 text-blue-800">
                                                <i class="fas fa-user-plus mr-1"></i>Added by me
                                            </span>
                                        </div>
                                        <% } %>
                                    </div>

                                    <!-- Property Details -->
                                    <div class="md:w-2/3">
                                        <div class="flex justify-between items-start mb-3">
                                            <div>
                                                <h3 class="text-xl font-bold text-gray-900 mb-2">
                                                    <%= property.title %>
                                                </h3>
                                                <p class="text-gray-600 mb-1">
                                                    <i class="fas fa-map-marker-alt mr-2 text-blue-500"></i>
                                                    <%= property.full_address || 'Location not specified' %>
                                                </p>
                                                <% if (property.seller) { %>
                                                <p class="text-gray-600 text-sm">
                                                    <i class="fas fa-user mr-2"></i>
                                                    Seller: <%= property.seller.name %>
                                                </p>
                                                <% } %>
                                            </div>
                                            <div class="text-right">
                                                <p class="text-2xl font-bold text-blue-600 mb-2">
                                                    <%= property.formatted_price %>
                                                </p>
                                                <% if (property.commissions && property.commissions.length > 0) { %>
                                                <p class="text-sm text-green-600">
                                                    <i class="fas fa-money-bill-wave mr-1"></i>
                                                    Total Commission: ₹<%= property.totalCommission.toLocaleString() %>
                                                </p>
                                                <% } %>
                                            </div>
                                        </div>

                                        <!-- Property Features -->
                                        <div class="grid grid-cols-2 md:grid-cols-4 gap-2 mb-4">
                                            <% if (property.bedrooms) { %>
                                            <div class="flex items-center text-gray-600">
                                                <i class="fas fa-bed mr-2"></i>
                                                <span><%= property.bedrooms %> Beds</span>
                                            </div>
                                            <% } %>
                                            
                                            <% if (property.bathrooms) { %>
                                            <div class="flex items-center text-gray-600">
                                                <i class="fas fa-bath mr-2"></i>
                                                <span><%= property.bathrooms %> Baths</span>
                                            </div>
                                            <% } %>
                                            
                                            <% if (property.built_up_area) { %>
                                            <div class="flex items-center text-gray-600">
                                                <i class="fas fa-ruler-combined mr-2"></i>
                                                <span><%= property.built_up_area %> <%= property.area_unit %></span>
                                            </div>
                                            <% } %>
                                            
                                            <% if (property.property_type) { %>
                                            <div class="flex items-center text-gray-600">
                                                <i class="fas fa-building mr-2"></i>
                                                <span><%= property.property_type %></span>
                                            </div>
                                            <% } %>
                                        </div>

                                        <!-- Actions -->
                                        <div class="flex flex-wrap gap-2 mt-4">
                                            <a href="/broker/properties/<%= property._id %>" 
                                               class="inline-flex items-center px-4 py-2 bg-blue-600 text-white font-medium rounded-lg hover:bg-blue-700 transition">
                                                <i class="fas fa-eye mr-2"></i>View Details
                                            </a>
                                            
                                            <% if (property.isAvailableForSale && property.status === 'live') { %>
                                            <a href="/broker/visits/schedule/<%= property._id %>" 
                                               class="inline-flex items-center px-4 py-2 bg-green-600 text-white font-medium rounded-lg hover:bg-green-700 transition">
                                                <i class="fas fa-calendar-alt mr-2"></i>Schedule Visit
                                            </a>
                                            <% } %>
                                            
                                            <% if (property.status === 'live' && property.cart_status?.in_cart) { %>
                                            <span class="inline-flex items-center px-4 py-2 bg-yellow-100 text-yellow-800 font-medium rounded-lg">
                                                <i class="fas fa-shopping-cart mr-2"></i>In Cart
                                            </span>
                                            <% } %>
                                            
                                            <% if (property.pendingCommission && property.pendingCommission > 0) { %>
                                            <span class="inline-flex items-center px-4 py-2 bg-green-100 text-green-800 font-medium rounded-lg">
                                                <i class="fas fa-money-bill-wave mr-2"></i>
                                                Pending: ₹<%= property.pendingCommission.toLocaleString() %>
                                            </span>
                                            <% } %>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <% }); %>
                    </div>
                <% } %>
            </div>

            <!-- Sidebar -->
            <div class="lg:col-span-1">
                <!-- Summary -->
                <div class="bg-white rounded-xl shadow p-6 mb-6">
                    <h2 class="text-xl font-bold text-gray-900 mb-4">Properties Summary</h2>
                    <div class="space-y-3">
                        <div class="flex justify-between items-center">
                            <span class="text-gray-600">Total Properties</span>
                            <span class="font-bold text-gray-900"><%= properties.length %></span>
                        </div>
                        
                        <% 
                            const statusCounts = {
                                pending_approval: 0,
                                approved: 0,
                                live: 0,
                                sold: 0,
                                rejected: 0
                            };
                            
                            properties.forEach(p => {
                                statusCounts[p.status] = (statusCounts[p.status] || 0) + 1;
                            });
                        %>
                        
                        <% Object.entries(statusCounts).forEach(([status, count]) => { 
                            if (count > 0) {
                                const statusColor = {
                                    'pending_approval': 'text-yellow-600',
                                    'approved': 'text-blue-600',
                                    'live': 'text-green-600',
                                    'sold': 'text-purple-600',
                                    'rejected': 'text-red-600'
                                }[status] || 'text-gray-600';
                        %>
                        <div class="flex justify-between items-center">
                            <span class="text-gray-600"><%= status.replace('_', ' ') %></span>
                            <span class="font-bold <%= statusColor %>"><%= count %></span>
                        </div>
                        <% } }); %>
                        
                        <!-- Added vs Assigned -->
                        <% 
                            const addedCount = properties.filter(p => p.addedByMe).length;
                            const assignedCount = properties.length - addedCount;
                        %>
                        <div class="pt-3 border-t border-gray-200">
                            <div class="flex justify-between items-center mb-2">
                                <span class="text-gray-600">Added by me</span>
                                <span class="font-bold text-blue-600"><%= addedCount %></span>
                            </div>
                            <div class="flex justify-between items-center">
                                <span class="text-gray-600">Assigned to me</span>
                                <span class="font-bold text-green-600"><%= assignedCount %></span>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Quick Actions -->
                <div class="bg-white rounded-xl shadow p-6">
                    <h2 class="text-xl font-bold text-gray-900 mb-4">Quick Actions</h2>
                    <div class="space-y-3">
                        <a href="/broker/properties/add" 
                           class="block w-full bg-blue-600 text-white text-center py-3 rounded-lg font-semibold hover:bg-blue-700 transition">
                            <i class="fas fa-plus-circle mr-2"></i>Add Property
                        </a>
                        <a href="/broker/visits" 
                           class="block w-full border border-green-600 text-green-600 text-center py-3 rounded-lg hover:bg-green-50 transition">
                            <i class="fas fa-calendar-day mr-2"></i>View Visits
                        </a>
                        <a href="/broker/commissions" 
                           class="block w-full border border-purple-600 text-purple-600 text-center py-3 rounded-lg hover:bg-purple-50 transition">
                            <i class="fas fa-money-bill-wave mr-2"></i>View Commissions
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>